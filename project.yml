name: WorkoutTracker
options:
  bundleIdPrefix: com.workouttracker
  deploymentTarget:
    iOS: "18.0"
  xcodeVersion: "15.0"
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: "5.9"
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"

packages:
  SQLite:
    url: https://github.com/stephencelis/SQLite.swift
    from: "0.15.0"
  SnapKit:
    url: https://github.com/SnapKit/SnapKit
    from: "5.7.0"

targets:
  WorkoutTracker-iOS:
    type: application
    platform: iOS
    deploymentTarget: "18.0"
    sources:
      - path: WorkoutTracker-iOS
        excludes:
          - "**/.DS_Store"
    settings:
      base:
        INFOPLIST_FILE: WorkoutTracker-iOS/App/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.drewburton.workouttracker
        TARGETED_DEVICE_FAMILY: "1,2"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_STYLE: Automatic
        UIApplicationShortcutItems:
          - UIApplicationShortcutItemTitle: "Start Workout"
            UIApplicationShortcutItemType: "StartWorkoutAction"
            UIApplicationShortcutItemIconSymbolName: "play.fill"
          - UIApplicationShortcutItemTitle: "Last Workout"
            UIApplicationShortcutItemType: "LastWorkoutAction"
            UIApplicationShortcutItemIconSymbolName: "clock"
    dependencies:
      - package: SQLite
        product: SQLite
      - package: SnapKit

schemes:
  WorkoutTracker-iOS:
    build:
      targets:
        WorkoutTracker-iOS: all
    run:
      config: Debug
    test:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
